fastapi>=0.110
uvicorn>=0.29
# The Modelgov Python SDK (packages/sdk-python). It is not yet published to
# PyPI, so install it from source (editable) from this repo:
-e ../../packages/sdk-python
# Once published, this becomes: modelgov
