{
  "name": "@modelgov/cli",
  "version": "1.6.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mml555/modelgov.git",
    "directory": "packages/cli"
  },
  "description": "Modelgov CLI for setup, operations, validation, and policy decisions.",
  "type": "module",
  "bin": {
    "modelgov": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelgov/policy-engine": "workspace:*",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
